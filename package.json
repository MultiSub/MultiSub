{
  "name": "multisubs-for-hbo-max",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.content.config.ts && vite build --config vite.page-hook.config.ts && vite build --config vite.popup.config.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mpd-parser": "^1.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.309",
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
