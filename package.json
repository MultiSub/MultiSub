{
  "name": "multisub-browser-extensions",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run build:hbo && npm run build:netflix",
    "build:hbo": "vite build --config vite.content.config.ts && vite build --config vite.page-hook.config.ts && vite build --config vite.popup.config.ts",
    "build:netflix": "vite build --config vite.netflix-content.config.ts && vite build --config vite.netflix-page-hook.config.ts && vite build --config vite.netflix-popup.config.ts",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mpd-parser": "^1.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.309",
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
